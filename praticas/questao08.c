/*
Faça um programa em C que calcule a altura alcançada por um avião após ter percorrido uma certa distância (seno(ângulo) = altura/distância). Considere o ângulo de inclinação do avião menor ou igual a 45°.
*/
#include <stdio.h>
#include <math.h>

int main() { 
  int valor_da_distancia = 100;
  int valor_do_angulo = 45 * 3.14 / 180;
  double sin(45 * 3.14 / 180);
  double final = valor_altura
  
  double altura = (valor_distancia * (sin(valor_do_angulo) = valor_altura / valor_distancia);

  printf("o valor da altura é %f\n ", valor_altura =  (valor_distancia * (sin(valor_do_angulo)) = valor_altura / valor_distancia);
  
 
  return 0

    
}
